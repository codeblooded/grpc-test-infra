package main_test

import (
	. "github.com/onsi/ginkgo"
	// "github.com/onsi/gomega"

	// "github.com/grpc/test-infra/containers/init/wait"
)

var _ = Describe("Wait", func() {
	It("parses podLabel flags into slices", func() {})
	It("returns zero when matching pods ready", func() {})
	It("returns non-zero if timeout exceeded", func() {})
	It("blocks with all pods missing", func() {})
	It("blocks with one pod missing", func() {})
})
